version: '3.8'
services:
  valkey:
    image: valkey/valkey:latest
    ports:
      - "6379:6379"
    volumes:
      - valkey-data:/data
    command: ["valkey-server", "--save", "60", "1", "--loglevel", "warning"]

volumes:
  valkey-data:
